@model IEnumerable<ProyectoMascotas.DAL.Models.Padecimiento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Padecimiento1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdMascotaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Padecimiento1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdMascotaNavigation.Genero)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPadecimiento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPadecimiento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPadecimiento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
